# PgBouncer test — verify connection pooling works
services:
  postgresql:
    image: postgresql:test
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: testpass
      PGBOUNCER_ENABLE: "true"
      PGBOUNCER_MAX_CLIENT_CONN: "100"
      PGBOUNCER_DEFAULT_POOL_SIZE: "10"
